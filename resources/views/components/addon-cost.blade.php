@if ($addon->title == 'Add-to-cart animation')
    @php
    $addon->cost = 39.48;
    $addon->conversion_rate = 0.12;
    @endphp
@endif

@if ($addon->title == 'Cart countdown')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Cart discount')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Cart goal')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Chat box')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Collection add-to-cart')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Color swatches')
    @php
    $addon->cost = 11.88;
    $addon->conversion_rate = 0.07;
    @endphp
@endif

@if ($addon->title == 'Cookie box')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Delivery time')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Discount Saved')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'F.A.Q page')
    @php
    $addon->cost = 23.88;
    $addon->conversion_rate = 0.07;
    @endphp
@endif

@if ($addon->title == 'Inventory quantity')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Linked options')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Live view')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Mega menu')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Newsletter pop-up')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Product tabs')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Product video')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Quick view')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Sales countdown')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Sales pop')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Shop protect')
    @php
    $addon->cost = 47.88;
    $addon->conversion_rate = 0.39;
    @endphp
@endif

@if ($addon->title == 'Skip cart')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Smart search')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Sticky add-to-cart')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Trust badge')
    @php
    $addon->cost = 59.88;
    $addon->conversion_rate = 0.74;
    @endphp
@endif

@if ($addon->title == 'Upsell bundles')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Upsell pop-up')
    @php
    $addon->cost = 17.88;
    $addon->conversion_rate = 0.03;
    @endphp
@endif

@if ($addon->title == 'Pricing table')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Wish list')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif

@if ($addon->title == 'Quantity break')
    @php
    $addon->cost = 35.88;
    $addon->conversion_rate = 0.15;
    @endphp
@endif